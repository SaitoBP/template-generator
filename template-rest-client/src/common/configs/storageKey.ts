/**
 * If you want to encrypt the data, set shouldEncode to true,
 * all data added to the storage will be encrypted.
 */
export const shouldEncode = false;

/**
 * The key used to store the user's settings.
 */
export const userSettingsKey = '$$GAME_NAME$$_user_settings';

/**
 * The key used to store the free bet data.
 */
export const freeBetKey = '$$GAME_NAME$$_free_bet';
