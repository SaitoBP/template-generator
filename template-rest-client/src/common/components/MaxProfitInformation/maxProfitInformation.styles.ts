import { AiFillInfoCircle } from 'react-icons/ai';
import styled from 'styled-components';

export const Icon = styled(AiFillInfoCircle)`
	width: 32px;
	height: 32px;
	outline: none;
	cursor: pointer;
`;
